import React from "react";
import ListOfGames from "components/Games/ListOfGames";

export default function Detail({ params }) {
  return <ListOfGames params={params} />;
}

// Hacer un slide con screenshots y meter mas info
