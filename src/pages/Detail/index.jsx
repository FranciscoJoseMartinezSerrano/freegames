import React from "react";
import SingleGame from "components/Games/SingleGame";

export default function Detail({ params }) {

  

  return <SingleGame params={params} />;
}

// Hacer un slide con screenshots y meter mas info
   